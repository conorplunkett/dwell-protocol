// SPDX-License-Identifier: MIT
pragma solidity 0.8.26;

// Convenience re-export for integrators reading this repo in isolation: the
// canonical interface is OpenZeppelin's, pinned via the lib/ submodule. Import
// from here or from @openzeppelin/contracts directly — they are the same type.
import {IERC20} from "@openzeppelin/contracts/token/ERC20/IERC20.sol";
